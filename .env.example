# Copy to .env and fill in before `docker compose up`.

# NOTE: login auth is currently disabled app-wide (see README's "Access control"
# section) -- these two are inert unless you re-enable it in app/main.py.
VAPT_ADMIN_USER=admin
VAPT_ADMIN_PASSWORD=

# Optional: free at https://wpscan.com/api -- enables CVE-level results for
# WordPress plugin/theme/core findings instead of just version enumeration.
WPSCAN_API_TOKEN=
